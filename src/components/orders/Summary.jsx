import React, { Fragment } from "react";

const Summary = (props) => {
  const products = props.products;
  return (
    <Fragment>
      <h2>Symmary and quantities</h2>
    </Fragment>
  );
};

export default Summary;
